import { ZeroWasteValidators } from './zero-waste-validators';

describe('ZeroWasteValidators', () => {
  it('should create an instance', () => {
    expect(new ZeroWasteValidators()).toBeTruthy();
  });
});
